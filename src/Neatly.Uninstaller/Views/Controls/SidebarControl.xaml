<UserControl x:Class="neatly.uninstaller.Views.Controls.SidebarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:neatly.uninstaller.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <DockPanel Grid.Row="1" Margin="10" VerticalAlignment="Center">
            <TextBlock Text="Installed Applications"
                       FontFamily="Segoe UI Semibold"
                       FontSize="14"
                       VerticalAlignment="Center"
                       DockPanel.Dock="Left" />
            <Separator Margin="10,0,0,0"
                       VerticalAlignment="Center"
                       Height="1"
                       DockPanel.Dock="Right" />
        </DockPanel>

        <ListBox Grid.Row="2"
                 ItemsSource="{Binding Apps}"
                 SelectedItem="{Binding SelectedApp, Mode=TwoWay}"
                 BorderThickness="0"
                 Background="Transparent"
                 HorizontalContentAlignment="Stretch"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.CanContentScroll="False"
                 HorizontalAlignment="Stretch">

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <local:AppItemControl
                        AppName="{Binding Name}"
                        AppIcon="{Binding Icon}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</UserControl>