<UserControl x:Class="Neatly.Uninstaller.Views.Controls.AppDetailsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="420"
             d:DesignHeight="320">

    <Border Background="{DynamicResource CardBackgroundBrush}"
            CornerRadius="10"
            Padding="16"
            Margin="20"
            BorderThickness="1"
            BorderBrush="#33FFFFFF">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="15" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="15" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="Selected Application"
                       FontSize="15"
                       FontWeight="SemiBold" />

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="48" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Image Source="{Binding SelectedApp.Icon}"
                       Width="48"
                       Height="48"
                       Stretch="UniformToFill"
                       ClipToBounds="True"
                       SnapsToDevicePixels="True" />

                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                    <TextBlock
                        Text="{Binding SelectedApp.Name}"
                        FontSize="20"
                        FontWeight="Bold" />
                    <TextBlock
                        Text="{Binding SelectedApp.Publisher}"
                        FontSize="13" />
                </StackPanel>
            </Grid>

            <StackPanel Grid.Row="4">

                <Separator Background="{DynamicResource SeparatorBrush}"
                           Margin="0,8" />

                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Publisher:" />
                    <TextBlock Grid.Column="1" Text="{Binding SelectedApp.Publisher}" />
                </Grid>

                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Version:" />
                    <TextBlock Grid.Column="1" Text="{Binding SelectedApp.Version}" />
                </Grid>

                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Install Location:" />
                    <TextBlock Grid.Column="1"
                               Text="{Binding SelectedApp.InstallLocation}"
                               TextWrapping="Wrap" />
                </Grid>

                <Separator Background="{DynamicResource SeparatorBrush}"
                           Margin="0,8" />

                <Grid Margin="10,15,10,0"
                      Height="50">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="10" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Foreground="White"
                            FontSize="16">

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="🗑️" Margin="0,0,8,0" FontSize="20" />
                            <TextBlock Text="Uninstall" FontWeight="450" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>

                    <Button Grid.Column="2"
                            Foreground="White"
                            FontSize="16">

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="📂" Margin="0,0,8,0" FontSize="20" />
                            <TextBlock Text="Open Folder" FontWeight="450" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </Grid>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>