<UserControl x:Class="Neatly.Uninstaller.Views.Controls.AppItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid Margin="6">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        
        <Border Width="40" Height="40" CornerRadius="8" Background="#3b3b5e" VerticalAlignment="Center">
            <Image Source="{Binding AppIcon, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Width="32" Height="32"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center" />
        </Border>
        
        <TextBlock Grid.Column="1"
                   Text="{Binding AppName, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   VerticalAlignment="Center"
                   FontWeight="SemiBold"
                   TextTrimming="CharacterEllipsis"
                   FontSize="15"
                   Margin="10,0" />
        
        <TextBlock Grid.Column="2"
                   Text="›"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="DimGray"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Right"
                   Margin="0,0,10,0" />
    </Grid>

</UserControl>