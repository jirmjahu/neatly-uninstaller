<UserControl x:Class="Neatly.Uninstaller.Views.Controls.TopBarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="100" d:DesignWidth="500">

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="450"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Height="30"
                Margin="10, 10,10,0"
                CornerRadius="5"
                Background="{DynamicResource SearchBackgroundBrush}"
                BorderBrush="{DynamicResource SearchBorderBrush}"
                BorderThickness="1">

            <DockPanel VerticalAlignment="Center">

                <TextBlock Text=""
                           FontFamily="Segoe MDL2 Assets"
                           Foreground="{DynamicResource SearchIconBrush}"
                           FontSize="12"
                           Margin="12,0,6,0"
                           VerticalAlignment="Center" />

                <TextBox Background="Transparent"
                         BorderThickness="0"
                         Foreground="{DynamicResource TextBrush}"
                         FontSize="15"
                         VerticalContentAlignment="Center"
                         Text="Search..."
                         CaretBrush="{DynamicResource TextBrush}" />
            </DockPanel>
        </Border>

        <StackPanel Grid.Column="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Margin="0,0,10,0">

            <Button Style="{StaticResource TopBarButtonStyle}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text=""
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="14"
                               Margin="0,3,6,0" />
                    
                    <TextBlock Text="Logs" FontSize="14" />
                </StackPanel>
            </Button>

            <TextBlock Text="|"
                       Opacity="0.35"
                       Margin="6"
                       VerticalAlignment="Center" />

            <Button Style="{StaticResource TopBarButtonStyle}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text=""
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="14"
                               Margin="0,3,6,0" />
                    
                    <TextBlock Text="Settings" FontSize="14" />
                </StackPanel>
            </Button>

            <TextBlock Text="|"
                       Opacity="0.35"
                       Margin="6"
                       VerticalAlignment="Center" />

            <Button Style="{StaticResource TopBarButtonStyle}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text=""
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="14"
                               Margin="0,3,6,0" />

                    <TextBlock Text="About" FontSize="14" />
                </StackPanel>
            </Button>

        </StackPanel>

    </Grid>
</UserControl>