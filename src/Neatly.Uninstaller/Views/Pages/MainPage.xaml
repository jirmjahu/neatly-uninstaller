<Page x:Class="Neatly.Uninstaller.Views.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:controls="clr-namespace:Neatly.Uninstaller.Views.Controls"
      mc:Ignorable="d">
    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="450" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>
        
        <controls:TopBarControl Grid.ColumnSpan="2" Grid.Row="0" />
        <controls:SidebarControl x:Name="Sidebar" Grid.Column="0" Grid.Row="1" />
        <controls:AppDetailsControl SelectedApp="{Binding SelectedApp, ElementName=Sidebar}" Grid.Column="1" Grid.Row="1" />
        <controls:BottomPanelControl Grid.Row="2"></controls:BottomPanelControl>
    </Grid>
</Page>
